<?xml version="1.0" encoding="utf-8"?>
<!--
  Política de rede do app. O padrão bloqueia http:// — token e senha só saem
  daqui por TLS. A exceção abaixo cobre a API rodando na máquina do
  desenvolvedor, que não tem certificado.

  Rodando em APARELHO FÍSICO apontado para o seu notebook (por exemplo
  --dart-define=API_URL=http://192.168.0.10:3333), acrescente esse IP como um
  <domain> aqui: esta lista casa por host exato, não aceita faixa de IP.

  Para um servidor de verdade, use https:// — nada precisa ser mudado aqui.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <!-- Emulador Android → máquina host. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
